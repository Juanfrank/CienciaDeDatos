/**
 * Proveedores de identidad: Azure AD y credenciales locales, normalizados a un mismo principal.
 *
 * MARCADOR DE POSICION. Este paquete existe en el esqueleto de monorepo (seccion 3.2)
 * con sus etiquetas de limites de dependencia ya aplicadas, pero su contenido se
 * implementa en: Entregable B.3 — Fase de cimiento.
 *
 * Secciones del contrato de ingenieria que lo definen: 4.7.1 (Azure AD/MSAL), 4.7.2 (cuentas locales, Argon2id, TOTP), 4.7.3 (IIdentityProvider).
 */
export const PENDIENTE_DE_IMPLEMENTACION = 'Entregable B.3 — Fase de cimiento' as const;
