{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "auth",
  "projectType": "library",
  "sourceRoot": "packages/auth/src",
  "tags": [
    "type:server",
    "scope:server"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/auth --max-warnings 0"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --dir packages/auth --passWithNoTests"
      }
    }
  }
}
