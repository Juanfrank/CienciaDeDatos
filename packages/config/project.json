{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "config",
  "projectType": "library",
  "sourceRoot": "packages/config/src",
  "tags": ["type:server", "scope:shared"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/config --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --dir packages/config --passWithNoTests" }
    }
  }
}
