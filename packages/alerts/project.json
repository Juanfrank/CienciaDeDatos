{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "alerts",
  "projectType": "library",
  "sourceRoot": "packages/alerts/src",
  "tags": ["type:server", "scope:server"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/alerts --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --dir packages/alerts --passWithNoTests" }
    }
  }
}
