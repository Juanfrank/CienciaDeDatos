{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "caching",
  "projectType": "library",
  "sourceRoot": "packages/caching/src",
  "tags": [
    "type:server",
    "scope:server"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/caching --max-warnings 0"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --dir packages/caching --passWithNoTests"
      }
    }
  }
}
