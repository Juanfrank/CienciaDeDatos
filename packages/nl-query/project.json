{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "nl-query",
  "projectType": "library",
  "sourceRoot": "packages/nl-query/src",
  "tags": ["type:util", "scope:shared"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/nl-query --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --dir packages/nl-query --passWithNoTests" }
    }
  }
}
