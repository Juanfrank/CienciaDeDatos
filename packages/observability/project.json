{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "observability",
  "projectType": "library",
  "sourceRoot": "packages/observability/src",
  "tags": ["type:server", "scope:server"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/observability --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --dir packages/observability --passWithNoTests" }
    }
  }
}
